# TEI 2026

Standalone website for The 1st Seminar of Towards Edge Intelligence (TEI).

Open `index.html` locally or deploy the repository with GitHub Pages.

For high-resolution poster export, run `python3 poster_export_server.py` and open
`http://127.0.0.1:8000/poster.html`.
